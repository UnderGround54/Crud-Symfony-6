<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home.index')}}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Accueil</font></font></a>
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
       {% if app.user %}
          <li class="nav-item">
          <a class="nav-link active" href="{{ path('ingredient.list') }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Ingredients
          </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('recipe.list') }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Recettes
            </a>
          </li> 
       {% endif %}  
      </ul>
      <div class="d-flex">
        <ul class="navbar-nav me-5">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ (app.user is same as(null)) ?  "Connexion" : app.user.Nom}}</a>
            <div class="dropdown-menu">
              {% if app.user %}
                <a class="dropdown-item" href="{{ path('user.update' , {'id': app.user.id})}}">Modifier mes informatios</a>
                <a class="dropdown-item" href="{{ path('password.update', {'id': app.user.id})}}">Modifier mot de passe</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('login.deconnexion')}}">Se deconnecter</a>
              {% else %}
                <a class="dropdown-item" href="{{ path('login.connexion')}}">Se connecter</a>
                <a class="dropdown-item" href="{{ path('login.inscription')}}">S'inscrir</a>
              {% endif %}
            </div>
          </li>
        </ul>
      </div>
      {# <form class="d-flex">
        <input class="form-control me-sm-2" type="text" placeholder="Chercher">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Chercher</font></font></button>
      </form> #}
    </div>
  </div>
</nav>